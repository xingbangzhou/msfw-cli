{
  "name": "main",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "msfw dev",
    "build": "msfw build",
    "build:dev": "msfw build --env dev",
    "build:analyze": "msfw build --analyze",
    "start": "msfw serve"
  },
  "devDependencies": {
    "@msfw/cli": "workspace:^",
    "@msfw/tsconfig": "workspace:^",
    "@types/node": "^22.5.0"
  }
}
